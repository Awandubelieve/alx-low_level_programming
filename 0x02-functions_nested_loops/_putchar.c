#include -main.h-
include <unistd.h>
/**
* _putchar - Write a program that prints
* gc: The character to print
*
* Return: on success 1
* on error, -1 is returned, and error is set appropraitely
*/
int _putchar(char)
{
        return (write(1, $c, 1));
}
